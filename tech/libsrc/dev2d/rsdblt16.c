// NON-LG: Rewrite of .asm file to C. Partially generated
// Original file name: rsdblt16.asm

#include <lg.h>
#include <assert.h>

typedef void (*memset_fcn)(uchar c, ushort *dst, int count);
typedef void (*memcpy_fcn)(ushort *dst, uchar *src, int count);

void flat16_rsd8_blit_init (memset_fcn mmset, memcpy_fcn mmcpy)
{
    // STUB: Needs code
    assert(0);
}

void flat16_rsd8_blit (uchar *rsd_src, uchar *dst, int grd_bm_row, int bm_w)
{
    // STUB: Needs code
    assert(0);
}

